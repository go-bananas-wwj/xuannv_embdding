_base_: harbin_128_stage2.yaml

experiment:
  name: harbin_128_stage2_quick
  use_wandb: false

training:
  epochs: 1
  save_every: 1
  eval_every: 1
  log_every: 1
  amp: true
  gradient_checkpointing: true

data:
  max_patches: 4
  batch_size: 2
  num_workers: 0   # 高分影像共享内存大，关闭 worker
