_base_: v1.1_distill_long_stable.yaml

experiment:
  name: v1.1_distill_long_stable_50ep
  wandb_run_name: "v1.1_distill_long_stable_50ep"

training:
  epochs: 50

model:
  target_heads:
    landsat_recon:
      weight: 2.0
    worldcover:
      weight: 1.0

data:
  cache_dir: /data/xuannv_embedding/cache
